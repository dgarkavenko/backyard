
		// if (ShotEffect_NDC != nullptr)
		// {
		// 	UNiagaraDataChannelWriter Writer = NiagaraDataChannel::WriteToNiagaraDataChannel(ShotEffect_NDC, SearchParams, 1, false, true, true, "Cannon Fire NDC Write");
		// 	Writer.WritePosition(n"Location", 0, MuzzleLocation_W);
		// 	Writer.WriteQuat(n"Rotation", 0, WorldRotation);
		// }

		// if (ShotEffect_NS != nullptr)
		// {
		// 	Niagara::SpawnSystemAttached(CannonData.ShotEffect_NS.Get(),
		// 								 GetOwner().RootComponent,
		// 								 NAME_None,
		// 								 MuzzleLocation_W,
		// 								 WorldRotation.Rotator(),
		// 								 EAttachLocation::KeepWorldPosition,
		// 								 true,
		// 								 true,
		// 								 ENCPoolMethod::AutoRelease);
		// }